// main.cpp
#include <mlcore/memory/allocator.h>

int main() {
	/*MLCore::Memory::ArenaAllocator allocator;
	float* data = allocator.allocate<float>(5);
	for (int i = 0; i < 5; i++) {
		data[i] = i;
	}
	allocator.deallocate<float>(data, 5);*/
}